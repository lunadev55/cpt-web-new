<h2>Carteiras</h2>
<div class="well well-lg small-size">Formulário para adicionar carteiras. </div>
<div class="row">
    <div class="col-md-4 well-lg"> <h3>Como Fundiona?</h3>
    <ul>
        <li>Selecione a moeda;</li>
        <li>Gere um endereço;</li>
        <li>Envie suas moedas.</li>
    </ul>
    <p class="warning well"> Por padrão nós não geramos endereços para os usuários.<br> Selecione uma moeda e clique em "gerar carteira".</p>
    <p>Em caso de dúvidas, contate o suporte.</p>
    </div>
    <div class="col-md-6 well well-lg">
        <ul class="nav nav-tabs">
          <li data-currency-options="BTC" class="active pointer histo_currency"><a class="padding-right-left-wallet">Bitcoin <% if !@total["BTC"].nil? %>(<%= @total["BTC"] %>)<% end %></a></li>
          <li data-currency-options="LTC" class="pointer histo_currency"><a class="padding-right-left-wallet">Litecoin <% if !@total["LTC"].nil? %>(<%= @total["LTC"] %>)<% end %></a></li>
          <li data-currency-options="ETH" class="pointer histo_currency"><a class="padding-right-left-wallet">Ethereum <% if !@total["ETH"].nil? %>(<%= @total["ETH"] %>)<% end %></a></li>
          <li data-currency-options="DOGE" class="pointer histo_currency"><a class="padding-right-left-wallet">Dogecoin <% if !@total["DOGE"].nil? %>(<%= @total["DOGE"] %>)<% end %></a></li>
        </ul>
        <div class="well">
            <%= form_tag nil, remote: true, class: "editForm" %>
            <%= hidden_field_tag :currency, @currency , class: 'hidden' %>
            <%= submit_tag "Gerar carteira", class: "submit btn btn-warning" %>
            </form>
            <div class="qrcode" align="center">
                <i class="fa fa-spinner fa-spin fa-5x" aria-hidden="true"></i>
            </div>
            <div class="wallets">
                
            </div>
        </div>
    </div>
    
    <div class="col-md-2"></div>
</div>